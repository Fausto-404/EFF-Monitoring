# ==========================================
# EFF-Monitoring v2.2.1 环境变量配置模板
# ==========================================
# 部署端只使用发布包内的离线镜像，不会下载或构建依赖。
# scripts/deploy.sh 首次启动时会自动生成数据库密码、JWT 密钥和管理员密码。
# ==========================================

EFF_API_IMAGE=eff-monitoring-api:v2.2.1
EFF_WORKER_IMAGE=eff-monitoring-worker:v2.2.1
EFF_WEB_IMAGE=eff-monitoring-web:v2.2.1
EFF_API_PORT=8000
EFF_WEB_PORT=8080

POSTGRES_PASSWORD=eff_password
DATABASE_URL=postgresql+psycopg://eff:eff_password@eff-postgres:5432/eff_monitoring
REDIS_URL=redis://eff-redis:6379/0

JWT_SECRET=replace-with-a-long-random-secret
INITIAL_ADMIN_USERNAME=admin
INITIAL_ADMIN_PASSWORD=admin123

CORS_ORIGINS=http://localhost:5173,http://localhost:8080
APP_TIMEZONE=Asia/Shanghai
ENABLE_DEMO_DATA=false
# DEMO_USER_PASSWORD=demo123456
